[package]
name = "solana-account-traits"
version.workspace = true
edition.workspace = true
authors = ["0xpluto <75406541+0xpluto@users.noreply.github.com>"]
license = "AGPL-3.0-only"
repository = "https://github.com/0xpluto/solana-protocols"
keywords = ["solana", "cache", "accounts", "traits"]
categories = ["cryptography::cryptocurrencies", "caching"]
description = "Shared traits and contracts for cache access and handler dispatch — the boundary between storage backends and protocol code"


[dependencies]
solana-pubkey = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
dashmap = { workspace = true }

[lints]
workspace = true
